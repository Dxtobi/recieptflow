services:
  - type: web
    name: receiptflow
    env: node
    buildCommand: echo "Build is part of start"
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
